import { ConvertTextPipe } from './convert-text.pipe';

describe('ConvertTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertTextPipe();
    expect(pipe).toBeTruthy();
  });
});
